import type { Config } from "tailwindcss"

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        cascadia: ['"Cascadia Code"', "monospace"],
      },
    },
  },
  plugins: [],
}
export default config
